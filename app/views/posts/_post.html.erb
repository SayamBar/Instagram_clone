<!DOCTYPE html>
<html lang="en">
<head>
  <title>Bootstrap Example</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<style>
.post-meta {
  background: #fff;
  text-align: left;
  font-weight: bold;
  padding: 15px;
  border: 1px solid #ddd;
}
.name{
  padding: 10px;
}
</style>



  <div class="post-meta">
    <div class="row">
      <div class="col-1">
        <%= link_to image_tag(post.user.avatar,height:40,width:40,class: "rounded-circle"), user_path(post.user.id) if post.user.avatar.attached? %>
      </div>
      <div class="col-11 name">
        <%= User.find(post.user_id).name %>
      </div>
    </div>
  </div>
  <% if post.image.attached? %>
    <div>
        <% if post.image.image? %>
            <%= link_to image_tag(post.image,class: "img-fluid"),post %>
        <% elsif post.image.video? %>
            <div>
                <%= video_tag(url_for(post.image), controls: "", class:"video img-fluid") %>
            </div>
        <% end %>
    </div>
  <% end %> 


</html>