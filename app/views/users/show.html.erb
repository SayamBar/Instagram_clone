<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Bootstrap Example</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
  </head>

  <body class="bg-light", style="margin-top:80px">
    <div class="row">
        <div class="col-3"></div>
        <div class="col-2">
            <% if @user.avatar.attached? %>
                <%= image_tag(@user.avatar,height:180,width:180,class: "rounded-circle") %> 
            <% else %>
                
            <% end %>
        </div>
            <div class="col-4 ">
                <div><%= @user.name %>
                <% if current_user == @user %>
                    <%= link_to "EditProfile", edit_user_registration_path, class: "nav-button fs-5 glyphicon glyphicon-user" %>
                <% end %>
                </div>
                <div><%= @user.email %></div>
                <div><%= @user.website %></div>
                <div><%= @user.bio %></div>
                <div><%= @user.gender %></div>
            </div>
        </div>
    </div><br><br>
      
    <div class="row">
        <div class="col-2"></div>
            <% (@user.posts).each do |post| %>
                <div class="col-2">
                    <%# <%= link_to image_tag(post.image,class: "img-fluid"),post %> 
                    <% if post.image.image? %>
                        <%= link_to image_tag(post.image,class: "img-fluid"),post %>
                    <% elsif post.image.video? %>
                        <div>
                            <%= video_tag(url_for(post.image), controls: "", class:"video img-fluid") %>
                        </div>
                    <% end %>
                </div>
            <% end %>
        </div>
    </div>
  </body>
</html>